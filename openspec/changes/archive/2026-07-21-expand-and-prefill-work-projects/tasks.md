## 1. Verified Directory Boundary

- [x] 1.1 Add a bounded read-only Rust command that batch-checks distinct local directory paths and register its typed IPC wrapper.
- [x] 1.2 Cover existing, missing, duplicate, empty, and over-limit directory requests with Rust tests.

## 2. Indexed Project Prefill

- [x] 2.1 Verify distinct indexed session directories and derive one fail-closed available-session set for all Work consumers.
- [x] 2.2 Merge available indexed Codex and Claude sessions into project groups, deduplicate provider session identities, and expose explicit Previous/Resume rows.

## 3. Project Disclosures

- [x] 3.1 Make each project heading an independent accessible disclosure that defaults expanded and never launches an agent.
- [x] 3.2 Make search reveal matches inside collapsed groups without changing their stored collapsed state.

## 4. Qualification and Closure

- [x] 4.1 Add focused Playwright coverage for existing and missing directories, prefill deduplication, project toggles, explicit resume, search reveal, and accessibility.
- [x] 4.2 Run Rust tests, TypeScript, Biome, focused Playwright, production frontend build, bundle budget, docs, and strict OpenSpec validation.
- [x] 4.3 Rebuild and inspect the native Mac app, update project status as implemented locally, and archive the completed change.
