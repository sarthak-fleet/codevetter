# Structural coverage fixture

This small Rust workspace and Swift extension set exercise cross-package symbol
isolation and cross-file extension extraction. The fixture is repository-owned,
network-free, and intentionally minimal so coverage regressions stay easy to
diagnose.
